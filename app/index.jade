doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Sample app

    //- must make this better
    //- for some reason I can't concat both script. there is a bug on safari. at least
    // build:js platform-min.js
    script(src='packages/web_components/platform.js')
    // endbuild

    // build:js dart_support-min.js
    script(src='packages/web_components/dart_support.js')
    // endbuild

    //- import the click-counter
    link(rel='import', href='click_counter/index.html')
    script(type='application/dart').
      export 'package:polymer/init.dart';

    //- I think there is no need for this.
    //- script(src='packages/browser/dart.js')

    // build:css index-min.css
    link(rel='stylesheet', href='index.css')
    // endbuild

  body
    h1 Base polymer (jade)
    p Hello world from Dart!
    #sample_container_id
      click-counter(count='5')

    // Google Analytics: change UA-XXXXX-X to be your site's ID
    // script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-XXXXX-X', 'auto');
      ga('send', 'pageview');
